{
  "name": "@bumpup/mono",
  "version": "1.0.0",
  "private": true,
  "description": "this package.json is just used to track dependencies of the individual packages of this monorepo",
  "scripts": {
    "install": "npx gulp install",
    "test": "npx gulp test",
    "build": "npx gulp build",
    "version": "npx gulp version",
    "publish": "npx gulp publish"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-shell": "^0.8.0"
  }
}
